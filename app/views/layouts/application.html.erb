<!DOCTYPE html>
<html>
  <head>
    <title>Kanban</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light mb-1" style="background-color: #e3f2fd;">
      <a class="navbar-brand" href="/">
        <%= image_tag('logo.svg', size: "30x30", alt: "Kanban Boards", class: 'd-inline-block') %>
        Kanban Boards
      </a>
     <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <span class="navbar-text mr-1">
            Four Demo Apps for Login:
        </span>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
              <a class="nav-link"
                href="https://kanban-1.projects.multimediatechnology.at/">1: No Login
                </a>
              <a class="nav-link current"
                href="https://kanban-2.projects.multimediatechnology.at/">2: basic auth</a>
              <a class="nav-link"
                href="https://kanban-3.projects.multimediatechnology.at/">3: devise</a>
              <a class="nav-link"
                href="https://kanban-4.projects.multimediatechnology.at/">4: omniauth</a>
          </div>
        </div>
      <span class="navbar-text float-xs-right">
        <% if current_user %>
        Logged in as <%= current_user.username %>
          <%= link_to "log out", logout_path %>
        <% else %>
          <%= link_to "log in", login_path %>
          | <%= link_to "register", new_user_path %>
        <% end %>
      </span>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </nav>
    <div class="container">
      <% if flash.any? %>
        <div style="position: absolute; top: 0; right: 0; width: 100vw; z-index: 1000;">
        <% flash.each do |key, value| %>
          <div role="alert" aria-live="assertive" aria-atomic="true" class="toast top-0">
            <div class="toast-header">
              <strong class="me-auto"><%= key.capitalize %></strong>
              <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
              <%= value %>
            </div>
          </div>
        <% end %>
        </div>
      <% end %>

    <%= yield %>
    </div>
    <footer class="bd-footer text-muted">
      <div class="container">
        <p>This Demo App offers basic login. Try username <kbd>mariam</kbd>, password <kbd>asecret</kbd>.</p>
        <p>You can find the source code in <a href="https://github.com/backend-development/rails-example-kanban-board-login/tree/version-2">the repository</a>.</p>
      </div>
    </footer>
  </body>
</html>
